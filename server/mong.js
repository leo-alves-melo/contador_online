/*var mongoose = require('mongoose');
mongoose.connect('mongodb://localhost/data_passagens');

var db = mongoose.connection;
db.on('error', console.error.bind(console, 'connection error:'));
db.once('open', function() {
  alert('conectado!')
});*/

